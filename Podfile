# Uncomment the next line to define a global platform for your project
 platform :ios, '10.0'

target 'unit-test-dependencies-tutorial' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for unit-test-dependencies-tutorial

  target 'unit-test-dependencies-tutorialTests' do
    inherit! :search_paths
    # Pods for testing
    pod 'Nimble'
  end

end
